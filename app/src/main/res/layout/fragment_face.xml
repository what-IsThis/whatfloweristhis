<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_background"
    tools:context=".FaceFragment"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        android:layout_gravity="center">
        <ImageView
            android:id="@+id/iv_faceFragment"
            android:layout_width="190dp"
            android:layout_height="190dp"
            android:src="@mipmap/ic_launcher"/>

        <ImageButton
            android:id="@+id/ib_faceFragment"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_alignBottom="@id/iv_faceFragment"
            android:layout_alignRight="@id/iv_faceFragment"
            android:src="@android:drawable/ic_menu_camera"/>
    </RelativeLayout>

    <TextView
        android:id="@+id/tv_faceFragment_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:layout_gravity="center"
        android:textSize="40dp"
        android:textStyle="bold"
        android:textAlignment="center"
        android:text="@string/title"
        android:textColor="@color/white" />
    <TextView
        android:id="@+id/tv_faceFragment_description"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="20dp"
        android:textAlignment="center"
        android:text="@string/description"
        android:textColor="@color/gray"/>

</LinearLayout>